import {Box} from '@mui/system'
import {styled} from '@mui/system'

const FixedBetween = styled(Box)({
    display:"flex",
    justifyContent:"center",
    alignItems:"center"
})

export default FixedBetween;